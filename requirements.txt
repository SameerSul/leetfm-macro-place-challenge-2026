# Runtime dependencies (mirror pyproject.toml `[project].dependencies`)
torch>=2.0.0
numpy>=1.20.0
matplotlib>=3.5.0
tqdm>=4.65.0
absl-py>=1.0.0
numba>=0.59

# Development / testing (mirror pyproject.toml `[project.optional-dependencies].dev`)
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0

# ML candidate-ranker training (mirror pyproject.toml `[project.optional-dependencies].ml`)
xgboost>=3.2.0
scikit-learn>=1.9.0; python_version >= "3.11"

# Optional baselines (mirror pyproject.toml `[project.optional-dependencies].baselines`)
# Uncomment if running torch-geometric-based reference placers.
# torch-geometric>=2.3.0
# scipy>=1.10.0
