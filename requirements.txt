# Core dependencies (mirror pyproject.toml `[project].dependencies`)
torch>=2.0.0
numpy>=1.20.0
matplotlib>=3.5.0
tqdm>=4.65.0
absl-py>=1.0.0

# Development / testing (mirror pyproject.toml `[project.optional-dependencies].dev`)
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0; python_version >= "3.8.1"

# Optional baselines (mirror pyproject.toml `[project.optional-dependencies].baselines`)
# Uncomment if running torch-geometric-based reference placers.
# torch-geometric>=2.3.0
# scipy>=1.10.0

# --- Runtime (src/placer/ + dreamplace_bridge/) ---
torch>=2.0.0
numpy>=1.20.0
numba>=0.59

# --- ML candidate-ranker training (src/placer/ml/, test/diagnostic/) ---
# Offline-only: trains the per-operator XGBoost rankers from ml_data/ traces.
xgboost>=3.2.0
scikit-learn>=1.9.0

# --- Tests ---
pytest>=7.0.0
