# GNN Routing-Fill Surrogate - Archived

This document is historical. It described a possible learned surrogate for the
deleted proxy optimizer. The active hierarchy-only placer has no `src/placer/ml/`
package and no R2 candidate loop for this surrogate to accelerate.

Do not implement against the old roadmap unless the proxy path is explicitly
restored. Future learning work should start from the current hierarchy pipeline
and define a hierarchy-specific objective before adding model code.
