# v2 submission dependencies
# --- Runtime (src/placer/ + dreamplace_bridge/) ---
torch>=2.0.0
numpy>=1.20.0
numba>=0.59

# --- ML candidate-ranker training (src/placer/ml/, test/diagnostic/) ---
# Offline-only: trains the per-operator XGBoost rankers from ml_data/ traces.
# Not imported on the submission's inference path.
xgboost>=3.2.0
scikit-learn>=1.9.0

# --- Tests (submissions/varrahan/v2/test/) ---
pytest>=7.0.0
